#include<iostream>
using namespace std;

main(){
	int change;
	cin>>change;
	if(change==1)
		cout<<"The temperature has changed by "
			<<change<<" degree"<<endl;
	else
		cout<<"The temperature has changed by "
			<<change<<" degrees"<<endl;
}

//Notamos que este codigo es un poco largo por lo que se puede reducir usando el conditional statement
